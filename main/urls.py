from django.conf.urls import url
# from .views import home, cafe_add, cafe_detail

urlpatterns = [
    # url(r'^$', home, name='home'),
    # url(r'^cafe/(?P<pk>\d+)$', cafe_detail, name='cafe_detail'),
    # url(r'^cafe/add/$', cafe_add, name='cafe_add'),
]